/*<%= Time.new %>*/
var pandaTweetSettings = {
  apiUrl : '<%= root_url %>',
  apiVrsn : '<%= api_version_number %>',
  apikey : '<%= apikey %>'
}

var pandaTweetThemeConfig = {
  cssUrl : '<%= stylesheet %>',
  wrapperHTML : '<%= raw wrapper_html %>',
  tweetHTML : function(item){
    return '<%= raw tweet_html %>';
  }
}

var pandaTweetScriptaBot = document.createElement("script");
pandaTweetScriptaBot.type = "text/javascript";
pandaTweetScriptaBot.src = '<%= widget_url %>';
document.body.appendChild(pandaTweetScriptaBot);