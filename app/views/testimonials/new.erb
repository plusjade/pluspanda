<% 
# params[:apikey] prioritizes an apikey call over a logged in user
# there should be no reason a logged-in user would pass apikeys unless he's on someone else's site.
%>
<% if !params[:apikey] && current_user %>
  <%= render 'editor.form' %>
<% else %>
  <%= render 'new.gatekeeper' %>
<% end %>
