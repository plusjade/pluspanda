/*<%= Time.new %>*/
var pandaSettings = {
  apiUrl : '<%= root_url %>',
  apiVrsn : '<%= api_version_number %>',
  apikey : '<%= apikey %>',
  fbUrl : '<%= facebox_url %>'
}

var pandaThemeConfig = {
  cssUrl : '<%= stylesheet %>',
  wrapperHTML : '<%= raw wrapper_html %>',
  testimonialHTML : function(item){
    return '<%= raw testimonial_html %>';
  }
}

var pandaScriptaBot = document.createElement("script");
pandaScriptaBot.type = "text/javascript";
pandaScriptaBot.src = '<%= widget_url %>';   
document.body.appendChild(pandaScriptaBot);