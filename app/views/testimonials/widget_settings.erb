/*<%= Time.new %>*/
var pandaSettings = {
  apiUrl : '<%= root_url %>',
  apiVrsn : '<%= api_version_number %>',
  apikey : '<%= user.apikey %>',
  theme : '<%= user.tconfig.theme %>',
  cssUrl : '<%= "#{root_url}/system/data/#{user.apikey}/#{user.tconfig.theme}.css" %>',
  structure : '<%= raw panda_structure %>',
  itemHtml : function(item){
    return '<%= raw item_html %>';
  }
}

